# UAT Run — v0.1 (partiel, sans device)

> **Type** : RUN DE VALIDATION RÉEL (mode autonome, Playwright MCP). Verdicts fermes, journal de `docs/UAT.md` mis à jour.
> **Date** : 2026-06-11 · **Heure** : ~16:30–16:38 UTC (18:30–18:38 CEST)
> **Companion** : v4.3.4 (Docker local, conteneur `companion-nvx-companion-1`, `Up ... (healthy)`, HTTP 200 sur http://localhost:8000)
> **Module NVX** : version « Dev » (`module-local-dev`, `/app/module-local-dev/companion-module-crestron-nvx`)
> **Device** : DM-NVX-360 (192.168.2.9) **au labo, NON joignable** → cas exigeant le device marqués `[-]`.
> **Branche** : `feature/capture-tooling`.

## Périmètre de ce créneau

Seuls les cas v0.1 **jouables sans device** ont reçu un verdict ferme : **A1 (AUTH-EMPTY)**, **C2 (CONN-UNREACHABLE)**, **B5 (LOG-VERBOSE)** (mécanisme). Tous les autres cas v0.1 nécessitent le DM-NVX-360 joignable et sont `[-]` ce créneau (raison indiquée par cas).

## Sécurité & discipline lockout (respectée)

- **Instance jetable** `UAT_V01_DISPOSABLE` (ID `oVu6j1LKQzqTeuu5TzQV-`) créée, pointée vers **192.0.2.1 (TEST-NET, injoignable)**. **Supprimée en fin de run** (vérifié : seule `Crestron_NVX` subsiste).
- **Instance réelle `Crestron_NVX` jamais touchée** : restée `enabled=false` du début à la fin → 0 appel réseau, 0 tentative d'auth contre le device réel → **budget lockout intact (0 échec d'auth consommé)**.
- Aucun mauvais mot de passe contre un device joignable. Les scénarios d'échec utilisent l'**injoignabilité réseau** (192.0.2.1, timeout), jamais un rejet d'auth. Aucun reboot.
- Note fuseau : les logs du conteneur sont en **UTC** ; l'UI Companion affiche en **CEST** (UTC+2). Toutes les preuves ci-dessous sont datées en UTC (source conteneur, autoritaire).

---

## Phase A — Gauntlet d'authentification

| ID | Verdict | Preuve retenue | Screenshot |
|---|---|---|---|
| **A1 · AUTH-EMPTY** | **`[x]` PASS** | Config = host `192.0.2.1` + user `admin` + **password VIDE**. Ré-init propre (Disable→Enable, Enable à `16:32:40.545Z`). Log frais dans la fenêtre de ré-init : `16:32:40.675Z [CONN] No password configured — not attempting login` puis `Module initialized successfully`. **Comptage `[AUTH]`/`[HTTP]` pour l'instance sur la fenêtre = 0** (vérifié `grep -cE "\[AUTH\]\|\[HTTP\]"` → `0`). Statut UI = **BadConfig** : badge `triangle-exclamation` `color=#fab92c` (ambre), popover au survol = **« Bad Configuration: No password configured »**. → Garde-fou agit **avant** le réseau ; BadConfig distinct d'AuthenticationFailure (aucun appel émis). Preuve fraîche, pas d'état vert résiduel. | `assets/2026-06-11-v0.1-partial/A1-badconfig-popover.png` |
| **A2 · AUTH-WRONG** | **`[-]`** | Exige un device **joignable** qui **rejette** le login (401/403) pour produire `AuthenticationFailure`. 192.168.2.9 absent. ⚠️ Lockout : 1 seule tentative le jour du labo. |
| **A3 · AUTH-GOOD** | **`[-]`** | Exige le device réel + bon mot de passe → statut vert + `[AUTH]` 200 frais. Device absent. |

### Détail A1 — fenêtre de ré-init (logs conteneur, UTC)

```
16:32:40.545Z info  Instance/Controller        Enable instance UAT_V01_DISPOSABLE
16:32:40.546Z info  Instance/ProcessManager    Starting instance: UAT_V01_DISPOSABLE
16:32:40.557Z info  Instance/Child/...         Process started process 1302
16:32:40.642Z info  .../Entrypoint             Module-host accepted registration
16:32:40.675Z error Instance/Connection/...    [CONN] No password configured — not attempting login
16:32:40.675Z info  .../Entrypoint             Module initialized successfully
```
Aucune ligne `[AUTH]` ni `[HTTP]` → l'appel réseau n'a jamais été tenté (garde-fou avant réseau). **PASS sur les deux critères** (statut BadConfig ET aucune tentative réseau).

---

## Phase B — Lectures sur session active

| ID | Verdict | Preuve retenue | Screenshot |
|---|---|---|---|
| **B1 · HB-NAME** | **`[-]`** | `device_name` lisible (mécanisme prouvé : variable vide sur instance injoignable), mais la **vérité terrain** (nom réel dans l'appareil) exige le device. |
| **B2 · HB-FW** | **`[-]`** | `firmware_version` lisible mais comparaison à `7.1.5259.00090` exige le device. |
| **B3 · HB-STATE** | **`[-]`** | `ip_address` lu correctement (`192.0.2.1`) et `connection_status` reflète l'erreur live — **mécanisme de lecture prouvé** — mais l'**état connecté réel** exige le device. |
| **B4 · FB-CONNECTED** | **`[-]`** | La **couleur active** du feedback exige l'état connecté → device. |
| **B5 · LOG-VERBOSE** | **`[x]` PASS (mécanisme)** | Comparaison A/B de **deux ré-inits appariés** sur la même instance injoignable. **OFF** (verbose décoché, ré-init à 16:35:56Z) : **3 lignes, niveau `error` uniquement** (`[HTTP] Timeout`, `[CONN] Connection failed`, `[HTTP] Network error`). **ON** (verbose coché + Save, fenêtre depuis 16:36:27Z) : **8 lignes, 5 `info` + 3 `error`**, avec des préfixes de flux **absents en OFF** : `[CONN] configUpdated() → reconnect`, `[CONN] connect() triggered`, `[AUTH] Login step 1 — GET /userlogin.html`, `[HTTP] GET /userlogin.html → sent`, `[CONN] Reconnect scheduled in 10s`. Le toggle agit : ON = nettement plus verbeux (préfixes `[AUTH]`/`[CONN]`/`[HTTP]` de niveau info ajoutés). Preuve fraîche (fenêtres horodatées). | `assets/2026-06-11-v0.1-partial/B5-log-tab-verbose.png` |

### Détail B5 — comparaison OFF vs ON (logs conteneur, UTC)

**Verbose OFF** (3 lignes, error only) :
```
16:36:02.143Z error [HTTP] Timeout: GET /userlogin.html
16:36:02.144Z error [CONN] Connection failed (192.0.2.1): NVX timeout: GET /userlogin.html
16:36:02.144Z error [HTTP] Network error: GET /userlogin.html — socket hang up
```
**Verbose ON** (8 lignes, 5 info + 3 error) :
```
16:36:33.841Z info  [CONN] configUpdated() → reconnect
16:36:33.842Z info  [CONN] connect() triggered
16:36:33.842Z info  [AUTH] Login step 1 — GET /userlogin.html
16:36:33.842Z info  [HTTP] GET /userlogin.html → sent
16:36:53.874Z error [HTTP] Timeout: GET /userlogin.html
16:36:53.875Z error [CONN] Connection failed (192.0.2.1): NVX timeout: GET /userlogin.html
16:36:53.875Z info  [CONN] Reconnect scheduled in 10s
16:36:53.876Z error [HTTP] Network error: GET /userlogin.html — socket hang up
```

> **Limite assumée** : sans device, les préfixes `[POLL]` (polling actif) et le détail complet du flux post-login ne sont pas observables. Le **mécanisme** du toggle est néanmoins prouvé fermement par la différence quantifiée (3 → 8 lignes, ajout du niveau `info` et des préfixes de flux). Ce n'est donc pas un PASS de complaisance, mais le périmètre reste « mécanisme » — la richesse complète sera confirmée le jour du labo.
> **Note non-régression** : le `[AUTH] Login step 1` ici est une tentative de login **contre l'hôte injoignable 192.0.2.1** (timeout après envoi du GET), **jamais** contre le device réel → 0 risque de lockout.

---

## Phase C — Disruptif & teardown

| ID | Verdict | Preuve retenue | Screenshot |
|---|---|---|---|
| **C1 · CONN-DROP** | **`[-]`** | Action physique `[HUMAN]` (couper réseau/alim du device) + observation des transitions vert→rouge→vert. Exige device + opérateur. |
| **C2 · CONN-UNREACHABLE** | **`[x]` PASS** | Config = host **192.0.2.1 (injoignable)** + user/pass **bidon non vides** (`admin`/`bogus-not-real`) → force une tentative réseau qui **timeout** (jamais un rejet d'auth). Save à 16:33:40Z. **Init non bloquant** : `Module initialized successfully` émis, UI réactive tout du long (snapshots OK, jamais figée sur « starting »). Statut UI = **erreur** : badge `triangle-exclamation` `color=#d50215` (rouge). **Pas de force-restart du process module** : sur toute la session depuis le ré-init A1, **exactement 1 `Process started` (PID 1302)** et **0** `exited`/`crash`/`Starting instance` répété (`grep -c "Process started"` → `1`). Les échecs de connexion se répètent à cadence contrôlée (~30 s) = retries normaux, **pas** une boucle de redémarrage de process. Cross-check variable (anti-faux-positif piège #2) : `connection_status` = **« Error: NVX timeout: GET /userlogin.html »** (live), `device_name`/`firmware_version` **vides** (aucun faux cache), `ip_address` = `192.0.2.1`. | `assets/2026-06-11-v0.1-partial/C2-connection-failure-status.png` |
| **C3 · CONN-DESTROY** | **`[-]`** | La preuve `GET /logout` puis silence exige une **session active** (donc device) avant destroy. Device absent → cas nominal non couvrable. (La mécanique destroy/suppression est par ailleurs exercée par le teardown de l'instance jetable, mais sans `/logout` puisqu'aucune session n'était établie.) |

### Détail C2 — preuve d'absence de force-restart (logs conteneur, UTC)

Cycle d'échec répété à cadence contrôlée (≈30 s), **sans** relance de process :
```
16:34:01.959Z error [HTTP] Timeout: GET /userlogin.html
16:34:01.960Z error [CONN] Connection failed (192.0.2.1): NVX timeout: GET /userlogin.html
16:34:31.997Z error [HTTP] Timeout: GET /userlogin.html
16:34:31.998Z error [CONN] Connection failed (192.0.2.1): NVX timeout: GET /userlogin.html
```
Historique process pour l'instance depuis le ré-init A1 :
```
16:32:40.557Z info Instance/Child/UAT_V01_DISPOSABLE Process started process 1302
(aucun autre "Process started" dans la fenêtre C2)
```
→ `Process started` total = **1**. Le symptôme du bug (relances répétées de `node`/`Starting instance`, « module restarted ») est **absent**. Critère « init non bloquant ET pas de boucle de redémarrage » **rempli**.

> Une seconde séquence `Process stopped`/`Process started process 1364` apparaît plus tard (~16:36:03Z) : elle est **attribuable à mon Disable→Enable explicite pour la phase B5** (ré-init volontaire), **pas** à un force-restart autonome. Hors fenêtre C2 — n'affecte pas le verdict.

---

## Synthèse des verdicts

| Cas | Verdict |
|---|---|
| A1 · AUTH-EMPTY | `[x]` PASS |
| A2 · AUTH-WRONG | `[-]` device requis |
| A3 · AUTH-GOOD | `[-]` device requis |
| B1 · HB-NAME | `[-]` device requis |
| B2 · HB-FW | `[-]` device requis |
| B3 · HB-STATE | `[-]` device requis |
| B4 · FB-CONNECTED | `[-]` device requis |
| B5 · LOG-VERBOSE | `[x]` PASS (mécanisme) |
| C1 · CONN-DROP | `[-]` device + humain requis |
| C2 · CONN-UNREACHABLE | `[x]` PASS |
| C3 · CONN-DESTROY | `[-]` device requis |

**3 cas jugés : 3 PASS, 0 FAIL, 0 douteux.** 8 cas `[-]` (device absent).

## Verdict global — GO/NO-GO (PARTIEL)

**GO PARTIEL.** Les 3 cas v0.1 jouables sans device passent fermement, avec preuves fraîches et horodatées (A1 garde-fou pré-réseau ; C2 init non bloquant + absence de force-restart prouvée par le comptage de process ; B5 mécanisme du toggle verbose par comparaison appariée). **Aucune régression observée** sur le périmètre testable.

⚠️ **Ce GO est partiel et NON suffisant pour un merge final** : les cas dépendant du device (A2, A3, B1–B4, C1, C3) **n'ont pas été validés**. Le verdict GO/NO-GO complet de non-régression v0.1 exige un **créneau labo avec le DM-NVX-360 joignable** pour dérouler les `[-]` ci-dessus, en respectant l'ordre non négociable et le budget lockout (A2 = 1 seule tentative).

## Nettoyage effectué

- Instance jetable `UAT_V01_DISPOSABLE` **supprimée** (vérifié : seule `Crestron_NVX` subsiste).
- Instance réelle `Crestron_NVX` **inchangée** (restée `enabled=false`, jamais activée).
- Navigateur fermé.
- Journal de `docs/UAT.md` **mis à jour** (ceci est un run réel).

## Annexe — screenshots

| Fichier | Contenu |
|---|---|
| `assets/2026-06-11-v0.1-partial/A1-badconfig-popover.png` | A1 : badge ambre + popover « Bad Configuration: No password configured » |
| `assets/2026-06-11-v0.1-partial/C2-connection-failure-status.png` | C2 : badge rouge `#d50215`, instance injoignable |
| `assets/2026-06-11-v0.1-partial/B5-log-tab-verbose.png` | B5 : onglet Log avec niveaux Info/Debug actifs, lignes verbeuses du module |
