• Session 2026-06-17 (HORS-LABO — hygiène + dette harness + cadrage v0.4). Labo indisponible → travail hors device.
• HYGIÈNE GIT : `feature/v0.4-audio` (jamais poussée, ~12 commits credential-chain local-only) **poussée sur origin** (backup, sans PR ni merge). Plan FIX1/FIX2 archivé (`3bbb3af`). 3 backups `backup/v0.3-pre-*` conservés (local-only, rôle éteint, zéro-perte non prouvé formellement).
• ✅ M4+M6 SOLDÉS (vague Team Dev sync TDD : architecte→test-writer→coder→QA+companion-reviewer). M6 = `HttpError` typé, détection 404 par `instanceof` (fin du match de chaîne fragile) `4e4b0bf`. M4 = bracket `try/finally logout()` sur les 5 méthodes oracle (fin fuite sessions) `c634d59`. **209/209 verts**, QA **GO**, revue **RAS**, poussés sur origin. (TODO restants : suivi (c) CFG-GOOD `status==='PASS'`, Task 3 fillConfig gated labo.)
• v0.4 CADRAGE : découvert que **le spec audio EST DÉJÀ VALIDÉ** (`docs/superpowers/specs/2026-06-17-v0.4-audio-design.md`) — les 3 questions sont tranchées (audio seul décodeur ; Volume/Mute via `AudioVideoInputOutput.Outputs[0].Ports[0].Audio` −100..100 ; branche v0.4-audio). RIEN n'est codé. Tranche hors-labo prête à planifier : moteur de fade (fonction pure, loi isolée `distributeSteps()`) + fake étendu + tests unitaires.
• NEXT v0.4 : Didier relit le spec (ouvert Cursor) → puis **writing-plans** de la tranche hors-labo. Points à challenger : UX gain brut −100..100, mute gated 7.1.5259, défaut `fadeMs=0`. Gated labo (toujours) : effet réel chemin, loi échelle, mute natif, débit POST, + Étape 0 flux audio décodé sur `.9` (lié #5).

• Session 2026-06-17 (CORRECTIFS HARNESS — anti-tâtonnement). Audit multi-agents (architecte+explorer+code-reviewer) → cause racine de la classe « config à double source de vérité » confirmée DANS LE CODE, puis plan TDD 9 tâches (`docs/superpowers/plans/2026-06-17-uat-harness-hardening.md`) exécuté en **subagent-driven + revue indépendante par tâche + revue finale (READY TO MERGE)**. **192 tests verts.**
• CORRIGÉ : #1/#2 login figé `admin` (4 steps codaient `username:'admin'` au lieu de `ctx.config.nvxUser`) + verrou e2e ; #3 gardes de rôle (writeStep/writeStepRx/scenario/baseline → SKIP) + scénario fake-only via `UAT_FAKE=1` ; #1b 5 clés décodeur ajoutées à layout.json (POST enfin émis) ; erreurs franches loadLayout/readVar. **#1 reconnexion = PAS un bug module** (`configUpdated` lit `secrets` et relance, vérifié `main.ts:80-89`). Task 3 (fillConfig) reportée labo. `src/` intouché.
• LIVRAISON : branche `fix/uat-credential-chain` (10 commits + doc) **mergée FF dans `feature/v0.4-audio`** (`6bc9e38`). Branche fix supprimable. PAS de merge vers develop (gate labo). 2 TODO différés : M6 (404 par match de chaîne) + M4 (`logout()` dette préexistante oracle.ts) — détaillés memory `session-2026-06-17-credential-chain-fix`.
• 🚦 NEXT = GATE LABO valide ces correctifs : `UAT_FAKE=1`, décodeur émet POST, SKIP au bon rôle, log `[CONN] configUpdated` (item E reconnexion). Puis reprise v0.4 audio sur la même branche.

• Session 2026-06-17 (LABO RÉEL — 1er run UAT sur matériel). Cadrage v0.4 audio (spec validée + miroir doc Crestron + recherche fade) puis bascule labo. **1er `uat:journey` sur device réel** (run `#03-Rx` : 7 PASS / 5 FAIL / 13 SKIP). Tout détaillé dans memory `lab-2026-06-17-realdevice-findings` (= LIRE EN PREMIER pour les correctifs).
• BACKLOG PRIORISÉ (corriger 1 à 1) : **#1** reconnexion non reprise après auth-refused+correction mdp (cause racine → CFG-GOOD warning, device_role vide, cascade) ; **#1b** boutons décodeur → **AUCUN POST** (symptôme observé) ; **#2** username form non appliqué (resté `admin`, masqué car device accepte admin) ; **#3** ENC writes FAIL au lieu de SKIP (fix 18e7595 incomplet) ; **#4** boutons décodeur absents de layout.json ; **#5** encodeur `.10` ne diffuse pas (CodecReady=false, codec ne s'arme pas — device ; bloque mesures audio v0.4).
• VALIDÉ SUR RÉEL : install, CFG-NOPASS→BadConfig, CFG-UNREACHABLE→ConnectionFailure, baseline, teardown, **détection de rôle** (role=Receiver). Unicast RTSP route ; multicast non routé.
• ⚠️ NON COMMITÉ : fix username (run-journey.ts/types.ts — `NVX_USER`), section v0.3 dans docs/UAT.md, docs/crestron-api/ (miroir), spec v0.4 (2026-06-17-v0.4-audio-design.md), run #03-Rx. ⚠️ DEVICES ALTÉRÉS : `.10` password off + Hdcp FollowInput + StreamSource=Input1 + routage vidéo none→Input1 ; connexion `nvx-uat-rx` laissée (username admin, à nettoyer). `.env.local`=docs/.env.local.
• NEXT = session de correctifs : attaquer #1 d'abord (lire step CFG-GOOD + logique reconnexion/secrets module), puis #2→#4 (harness), #5 = device/Support. Re-runs ciblés (⚠️ lockout : CFG-WRONGPASS = 1 échec/run).

• Session 2026-06-17 (créneau 2). Relance complète tests : **158 structurels verts** (+3 resolveRunDir) + **journeys RX & TX rejouées contre 2 fakes frais → 0 FAIL** (RX 18 PASS `#01-Rx`, TX 15 PASS `#02-Tx`). 3 commits sur feature/v0.3-decoder : **18e7595** fix(uat) ENC steps SKIP (pas FAIL) sur mauvais rôle (symétrie décodeur — fin des « FAIL attendus » ambigus) ; **e14f93a** feat(uat) dossier de run versionné `date#NN-Module` (resolveRunDir + deriveModule) ; **0a49d13** chore(uat) rapports de run versionnés dans git (.gitignore, passe anti-secret OK).
• 🟢 DIAGNOSTIC FINAL : le **module charge bien** (role=Receiver) ET l'environnement est sain. Le « timeout `/Device/*` » du créneau précédent venait d'un **environnement churné** (fakes pas démarrés / git switch sous Companion live), **PAS d'une dégradation du fake** (hypothèse erronée supprimée) ni du code — **CONFIRMÉ** : 2 fakes frais ont encaissé les 2 journeys sans aucun timeout (logs fakes : 0 erreur).
• Toutes les branches de travail POUSSÉES sur origin (v0.3-decoder, v0.2.1, v0.2-encoder + main/develop/capture-tooling) ; 3 backups restent locaux ; pas de PR (gate UAT labo avant merge develop).
• NEXT = (A) **démarrer v0.4 Audio/Vidéo** — point de départ + 3 questions de cadrage dans memory `project-v0.4-kickoff` (branche parente / chemins JSON audio / périmètre audio vs audio+vidéo) ; (B) gate UAT au labo réel (3 NVX) puis merge progressif du stack. ⚠️ ne pas `git switch` pendant Companion live (recharge le module).
• Session 2026-06-16. v0.3 DECODER CODE COMPLET (spec→plan→9 tâches Subagent-Driven, double-revue spec+qualité par tâche). + RÉALIGNEMENT « frère & sœur » encoder↔decoder (audit architecte) extrait sur une branche v0.2.1. **136 tests verts**, build clean. Reste = gate UAT labo.
• NOUVELLE TOPOLOGIE : develop ← v0.2-encoder ← **v0.2.1** (fondation multi-endpoint + durcissement encodeur) ← **v0.3-decoder** (décodeur pur, rebasé). Backup : backup/v0.3-pre-realign. SÉQUENCE LABO : v0.2 → v0.2.1 → v0.3.
• Session 2026-06-14/15 (très dense). CONSOLIDÉ : harness UAT + fake device + presets + matrice **fusionnés dans feature/v0.2-encoder** (fast-forward, uat-harness supprimée). Parcours complet 14/14 contre le fake. Stack initiale : develop ← v0.2-encoder ← v0.3-decoder.
• Roadmap : GATE capture ✅ → v0.2 Encoder (CODE FAIT, UAT à venir) → v0.2.1 (align) → v0.3 Decoder (CODE FAIT) → v0.4 Audio/Vidéo → v0.5 Device-Ops → v1.0
• Harness — PARCOURS UTILISATEUR + 4 outils (REST Companion · logs docker · REST device=oracle · Chromium=Chrome système). Spec specs/2026-06-14-uat-harness-design.md (78a6dc6) ; plan plans/2026-06-14-uat-harness-journey.md (ff16f72).
• Wave 1+2+3+4 FAITES (scripts/uat/) : harness « livrable », 87 tests unitaires verts. Parcours LOCAL 3/3 PASS LIVE (INSTALL, CFG-NOPASS→warning, CFG-UNREACHABLE→error). Parcours complet 14 steps ; dry-run UAT_LAB=1 sans device = 3 PASS / 11 SKIP / 0 FAIL.
• Wave 3 : steps-lab.ts = CFG-WRONGPASS + CFG-GOOD (auth) · BASELINE · CAP/ENC-VARS/ENC-FEEDBACKS/ENC-NAME/ENC-MULTICAST/ENC-ENABLE/ENC-DISABLE (USE=v0.2 encodeur) · TEARDOWN. WRITE via JourneyConfig.layout (bouton non mappé → SKIP).
• Wave 4 : fixtures/layout.json + docs/uat-runs/SETUP.md (runbook) + lib/escalate.ts (glue escalade LLM, pure). RETRAIT des cases/{auth,encoder} + run.ts tier-0 périmés + scripts npm uat/uat:tier0 (dé-duplication ; couverture contrat conservée dans tests module + oracle). Seul script UAT = uat:journey.
• DÉCISION : on réutilise le Companion du compose `companion-nvx-companion-1` (:8000, monte déjà le repo via module-local-dev). Connexion test `nvx-uat` créée (host 192.0.2.1). Config : COMPANION_CONTAINER=companion-nvx-companion-1, UAT_LABEL=nvx-uat.

• FAKE DEVICE (idée Didier) : scripts/uat/fake-device/server.ts rejoue les RAW réels (.10 Transmitter) + auth/SetPartial → finalise le script SANS device. npm uat:fake-device. Parcours UAT_LAB=1 contre le fake = 10 PASS / 0 FAIL / 4 SKIP. Lancer : FAKE_NVX_PASS=test123 npm run uat:fake-device & puis UAT_LAB=1 NVX_HOST=host.docker.internal NVX_PORT=8443 ORACLE_HOST=127.0.0.1 NVX_PASS=test123 UAT_LAYOUT='{}' COMPANION_CONTAINER=companion-nvx-companion-1 npm run uat:journey.
• 3 bugs révélés par le fake (corrigés) : catégorie REST saine = 'good' pas 'ok' ; non-idempotence (enable avant fillConfig) ; variables 404 si lues trop tôt (readVar poll). nvx-uat config = host.docker.internal:8443.
• LIFECYCLE connexion auto : run-journey delete-si-existe+create au démarrage, delete à la sortie (UAT_KEEP=1 pour garder). createConnection + deleteConnectionViaUi (Chromium). Pas de DELETE REST (vérifié doc #4101/#4047). ⚠️ recréer = nouvel ID → casse boutons WRITE → UAT_KEEP=1.
• PRESETS encodeur livrés (v0.2) : 4 presets section Encoder, API SDK v2 (type:'simple' + CompanionPresetSection[], setPresetDefinitions(structure,presets)). 91 tests. Vérifié LIVE après docker restart conteneur (⚠️ restart connexion ≠ recharge code module).
• MATRICE FONCTIONNELLE figée : docs/functional-matrix.md (Actions/Feedbacks/Variables par fonctionnalité v0.2→v0.5). Design validé : config=connexion seule ; set-once=action+preset ; fade volume v0.4 (set_volume/adjust_volume + fadeMs, rampe module-side log/dB, NVX sans fade natif). Recherche web confirme v0.2 idiomatique (Magewell Ultrastream = jumeau).

• ✅ 14/14 CONTRE LE FAKE (2026-06-15) : parcours complet PASS 14 / FAIL 0 / SKIP 0 (A1 : Didier a glissé les 4 presets Encoder + édité name=UAT-STREAM/mcast=239.200.0.1, run UAT_KEEP=1). Chaîne WRITE bouton→module→fake→oracle prouvée. Fix CFG-UNREACHABLE warm (poll 30s, regex élargie, 2658b06). 91 tests.

▶ STRATÉGIE (Didier) : développer v0.3→v0.5 hors-labo (chacun avec son fake construit depuis les RAW : .9 Receiver pour v0.3, audio .10 pour v0.4…). Au labo : switch v0.2→test→v0.3→test→… ; si tout OK, merge progressif du stack. Garde-fous : fake depuis RAW (pas inventé), marquer 🔜 ce qui n'est pas capturé, prévoir Transmitter+Receiver au labo.
▶ v0.3 Decoder — CODE COMPLET ✅ (spec 472a72d, plan 2026-06-15-v0.3-decoder.md). FULL scope, routing gouverné par SessionInitiation, fake Receiver (RAW .9) + steps UAT DEC-*. Revue finale GO. PARITÉ encoder↔decoder atteinte : garde post()/Processing, useVariables, ids enc_*/dec_*, couleurs locales.
▶ v0.2.1 (align) — CODE COMPLET ✅ : fondation multi-endpoint (contrat helpers + poll aux), encodeur durci (garde Processing + stream_processing var/feedback, useVariables, rename enc_enable/disable_stream — pas d'UpgradeScript car pré-release). Revue GO, 95 tests. main.ts robustesse v0.1/v0.2 INTACTE.
▶ NEXT = GATE UAT LABO (seul reste). Séquence : tester v0.2 (Transmitter) → v0.2.1 (Transmitter, vérifier rename+useVariables+garde) → v0.3 (Receiver, fake déjà OK local ; au labo : DEC-* boutons + UAT_KEEP=1). Si vert → merge progressif du stack vers develop.
▶ VARIABLES tx_ FAIT (6b77396 sur v0.2.1, v0.3 re-rebasée) : stream_*/encoder_url → tx_* (symétrie tx_/rx_). Feedback IDs non renommés (namespace distinct). Backups : backup/v0.3-pre-realign + backup/v0.3-pre-rerebase (supprimer quand serein).
▶ BACKLOG ALIGN restant (optionnel) : IDs feedback encodeur non préfixés tx_ (stream_enabled…) ; garde Processing droppe le write sans log (2 panneaux ; exposer logger via helpers).
▶ BACKLOG À RÉFLÉCHIR (Didier 15/06) : les hors-périmètre du §9 spec v0.3 qui ne sont récupérés par AUCUNE version planifiée → mode `ByTransmitter` (push) + sélection stream secondaire `Streams[1..3]`. Décider explicitement (implémenter plus tard / ticket / abandon assumé), ne pas laisser tomber en silence. (mode toggle→v0.5, audio→v0.4 sont déjà couverts.)
▶ Env chaud : fake Transmitter (bg) + nvx-uat (4 boutons USE) — à recycler/arrêter selon besoin.
▶ NOTE HYGIÈNE : presets + matrice = code/doc MODULE committés sur feature/uat-harness (à cherry-pick ligne v0.2 au merge).
▶ NEXT v0.2 = UAT au labo (device, mode Transmitter). Backlog : docs/VALIDATION.md.
▶ Env live : Companion compose `companion-nvx-companion-1` sur :8000 (3 instances NVX réelles + nvx-uat de test). playwright-core global+npm link, Chrome. ⚠️ NE PAS lancer un conteneur SÉPARÉ sur 8000 (incident Wave 0) — réutiliser le compose. 2 vulns npm high (playwright-core) à voir à froid.
▶ Jetables nettoyés. fake-device/certs/ gitignoré (cert self-signed généré).
