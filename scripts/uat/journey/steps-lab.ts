import type { JourneyStep } from './types.js'

/**
 * Lab steps (scope: 'lab') need a reachable device. Filled in Wave 3
 * (wrong-password, good-password, baseline, USE = v0.2 encoder, teardown).
 * Stubbed empty for now so the journey runner compiles and the local steps run.
 */
export const labSteps: JourneyStep[] = []
